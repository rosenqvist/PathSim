#pragma once

namespace pathsim {

struct ViewSettings {
    bool show_heatmap{false};
    bool show_path_direction{false};
    bool show_tooltips{true};
    bool show_help{false};
};

} // namespace pathsim